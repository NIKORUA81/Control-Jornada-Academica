{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2016",
    "lib": ["es6"], // Considera añadir "dom" si usas APIs del DOM, aunque para backend es raro. "esnext" es otra opción.

    /* Modules */
    "module": "commonjs",
    "rootDir": "src",
    "resolveJsonModule": true,

    /* JavaScript Support */
    "allowJs": true,

    /* Emit */
    "outDir": "dist",

    /* Interop Constraints */
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,

    /* Type Checking */
    "strict": true,
    "noImplicitAny": false, // Considera cambiar a true para mayor seguridad de tipos.

    /* Completeness */
    "skipLibCheck": true
    // Se han omitido las opciones comentadas del archivo original para brevedad,
    // pero las opciones activas clave se han mantenido.
  },
  "include": [
    "src/**/*" // Solo incluye archivos dentro del directorio src para la compilación principal
  ],
  "exclude": [
    "node_modules",
    "dist", // Directorio de salida
    "tests"   // Excluye el directorio de pruebas de la compilación `tsc`
  ]
}
